<!-- MIGRATION_CLASSIFICATION: NOT_RECOMMENDED -->
<!-- This legacy Visualforce page is a candidate for later Flow/LWC evaluation. -->
<apex:page controller="LeadQualificationController" tabStyle="Account" lightningStylesheets="true">
    <apex:sectionHeader title="Scoring Admin" subtitle="Legacy console"/>
    <apex:pageMessages/>
    <apex:form>
        <apex:pageBlock title="Scoring Admin">
            <apex:pageBlockButtons>
                <apex:commandButton value="Refresh" action="{!refresh}"/>
                <apex:commandButton value="Recalc" action="{!recalc}" rendered="{!NOT(ISBLANK(selectedIds))}"/>
            </apex:pageBlockButtons>
            <apex:pageBlockSection columns="2">
                <apex:inputText value="{!searchKey}" label="Search"/>
                <apex:commandButton value="Search" action="{!search}"/>
            </apex:pageBlockSection>
            <apex:pageBlockTable value="{!rows}" var="row">
                <apex:column>
                    <apex:inputCheckbox value="{!row.selected}"/>
                </apex:column>
                    <apex:column value="{!row.label}"/>
                    <apex:column value="{!row.status}"/>
            </apex:pageBlockTable>
        </apex:pageBlock>
    </apex:form>
</apex:page>
