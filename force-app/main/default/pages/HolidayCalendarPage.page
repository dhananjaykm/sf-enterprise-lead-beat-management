<!-- MIGRATION_CLASSIFICATION: LWC_REQUIRED -->
<!-- This legacy Visualforce page is a candidate for later Flow/LWC evaluation. -->
<apex:page controller="LeadAssignmentConsoleController" tabStyle="Account" lightningStylesheets="true">
    <apex:sectionHeader title="Holiday Calendar" subtitle="Legacy console"/>
    <apex:pageMessages/>
    <apex:form>
        <apex:pageBlock title="Holiday Calendar">
            <apex:pageBlockButtons>
                <apex:commandButton value="Refresh" action="{!refresh}"/>
                <apex:commandButton value="Save" action="{!save}" rendered="{!NOT(ISBLANK(selectedIds))}"/>
            </apex:pageBlockButtons>
            <apex:pageBlockSection columns="2">
                <apex:inputText value="{!searchKey}" label="Search"/>
                <apex:commandButton value="Search" action="{!search}"/>
            </apex:pageBlockSection>
            <apex:pageBlockTable value="{!rows}" var="row">
                <apex:column>
                    <apex:inputCheckbox value="{!row.selected}"/>
                </apex:column>
                    <apex:column value="{!row.label}"/>
                    <apex:column value="{!row.status}"/>
            </apex:pageBlockTable>
        </apex:pageBlock>
    </apex:form>
</apex:page>
