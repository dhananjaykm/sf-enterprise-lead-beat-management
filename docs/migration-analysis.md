# Migration analysis (summary)

Positive Flow candidates: field derivation, priority, duplicate flag, visit create, reminder tasks, notifications.

Partial: assignment orchestration, scoring weights.

Keep Apex: ScoringMathEngine, LeadScoringBatch, HTTP enrichment, recursive decay, trigger framework.

UI: BeatPlanningPage and LeadBulkReassignmentPage are Screen Flow candidates. Scoring visualization should remain LWC.
